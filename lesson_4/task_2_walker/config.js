"use strict";

/**
 * Объект содержащий настройки
 * @property {int} rowsCount - количество строк в игровом поле
 * @property {int} colsCount - количество колонок в игровом поле
 */
const config = {
    rowsCount: 10,
    colsCount: 10,
};